{{#each blogs as |blog|}}
  <div class="card bg-danger text-light" style="width: 18rem">
    <div class="card-body">
      <h5 class="card-title">{{blog.title}}</h5>
      <h6 class="card-subtitle mb-2">{{blog.user.name}}</h6>
      <p class="card-text">
      </p>
    </div>
  </div>
{{/each}}